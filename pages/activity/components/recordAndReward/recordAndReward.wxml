<view class="weui-cells__title">Don't want to code le, let it be</view>
<view class="weui-cells weui-cells_after-title">
  <view class="weui-cell" wx:for="{{records.items}}" wx:key="name">
    <view class="weui-cell__bd">{{item.name}}, {{item.value}}</view>
  </view>
</view>